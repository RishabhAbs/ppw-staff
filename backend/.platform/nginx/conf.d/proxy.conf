# Allow large image/video uploads through the nginx reverse proxy.
# Default is 1 MB, which rejected product-image POSTs with HTTP 413
# ("client intended to send too large body"). Product images run a few MB
# and short videos can be larger, so allow up to 50 MB.
client_max_body_size 50M;
